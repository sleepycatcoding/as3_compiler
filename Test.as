package
{
    public class Main
    {
        // a comment
        public function hello(mikkishoe:void):void
        {
            // Helo world
            var name = 10 + 10 * 1000 / 100 * (100 - 10000);
            var test = mikkishoe + name;

            var val = "Support string literal";
        }
    }
}